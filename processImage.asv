function processImage(dir, tabGroup)
    
    % Tempat Function
    img = imread(dir);
    rectlan = lan(dir);
    
    
    
    
    
    % eh Tempat Show
    for i = 1 : length(rectlan)
      rectangle('Position', rectlan(i), ...
     'Linewidth', 1, 'EdgeColor', 'r', 'LineStyle', '-'); 
    end
    
end

